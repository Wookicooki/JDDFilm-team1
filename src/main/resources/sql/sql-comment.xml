<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--  자유게시판-->
<mapper namespace="com.example.jangdocdaefilm.mapper.CommentMapper">
  <select id="freeCommentList" resultType="com.example.jangdocdaefilm.dto.CommentDto">
    SELECT
    content, create_dt, id, user_name, free_idx
    FROM
    comment
    WHERE
    delete_yn = 'N'
    ORDER BY
    idx ASC;
  </select>

  <insert id="writeFree" parameterType="com.example.jangdocdaefilm.dto.FreeDto">
    INSERT INTO
    free (title, content, id, user_name, image, create_dt)
    VALUES
    (#{title}, #{content} , #{id}, #{userName}, now(), now())
  </insert>

  <select id="updateFreeView" resultType="com.example.jangdocdaefilm.dto.FreeDto" parameterType="int">
    SELECT
    idx, title, content, image, create_dt, user_name, id, hit_cnt, delete_yn
    FROM
    free
    WHERE
    idx = #{idx}
    AND
    delete_yn = 'N'
  </select>

  <update id="updateFree" parameterType="com.example.jangdocdaefilm.dto.FreeDto">
    UPDATE
    free
    SET
    title = #{title} , content = #{content}, image = now()
    WHERE
    idx = #{idx}
  </update>

  <update id="deleteFree" parameterType="int">
    UPDATE
    free
    SET
    delete_yn = 'Y'
    WHERE
    idx = #{idx}
  </update>

  <update id="updateFreeHitCount" parameterType="int">
    UPDATE
    free
    SET
    hit_cnt = hit_cnt + 1
    WHERE
    idx = #{idx}
  </update>
</mapper>
