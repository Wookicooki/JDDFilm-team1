<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!--  현재 상영작-->
<mapper namespace="com.example.jangdocdaefilm.mapper.NowMapper">
  <select id="selectNowList" resultType="com.example.jangdocdaefilm.dto.NowDto">
    SELECT
    idx, title, id, create_dt, hit_cnt
    FROM
    now
    WHERE
    delete_yn = 'N'
    ORDER BY
    idx DESC;
  </select>

  <select id="selectNowDetail" resultType="com.example.jangdocdaefilm.dto.NowDto" parameterType="int">
    SELECT
    idx, title, content, image, create_dt, update_dt, id, hit_cnt, delete_yn
    FROM
    now
    WHERE
    idx = #{idx}
    AND
    delete_yn = 'N'
  </select>

  <insert id="insertNow" parameterType="com.example.jangdocdaefilm.dto.NowDto">
    INSERT INTO
    now (title, content, id, create_dt)
    VALUES
    (#{title}, #{content} , #{createId} ,now())
  </insert>

  <update id="updateNow" parameterType="com.example.jangdocdaefilm.dto.NowDto">
    UPDATE
    now
    SET
    title= #{title} , content= #{content}, id= #{Id}, update_dt= now()
    WHERE
    idx = #{idx}
  </update>

  <update id="deleteNow" parameterType="int">
    UPDATE
    now
    SET
    delete_yn = 'Y'
    WHERE
    idx = #{idx}
  </update>

  <update id="updateNowHitCount" parameterType="int">
    UPDATE
    now
    SET
    hit_cnt = hit_cnt + 1
    WHERE
    idx = #{idx}
  </update>
</mapper>
